{
  "name": "@fluentflow/server",
  "version": "0.1.0",
  "private": true,
  "description": "Sync API and Anki import service for FluentFlow",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "node --watch src/index.ts",
    "start": "node dist/index.js",
    "test": "node --test test/*.test.ts"
  },
  "dependencies": {
    "@fluentflow/core": "*",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "firebase-admin": "^14.4.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^24.0.0",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=26.1.0"
  },
  "license": "MIT"
}
