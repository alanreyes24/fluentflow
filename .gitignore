node_modules/
dist/
build/
# ...except the desktop packaging inputs (app icon, entitlements), which are
# committed source, not build output.
!apps/desktop/build/
.expo/
.expo-shared/
*.tsbuildinfo
.env
.env.local
*.log
npm-debug.log*
yarn-error.log
.DS_Store
Thumbs.db

# Firebase service credentials
**/serviceAccount*.json
!**/serviceAccount.example.json

# Test scratch
packages/core/test/.fixtures/

# Model weights fetched by scripts/fetch-desktop-model.mjs, and any local
# copies kept for testing. Gigabytes; never in git.
.model-cache/

# Build output and evidence from scripts/verify-web.mjs
.web-export/
.web-export-shots/
.desktop-shots/
# `expo export --platform ios`, run to confirm the Hermes bundle still builds
.ios-export/

# Generated by `npm run sample-deck`, for importing on a device
sample-deck-*.apkg
